(ns crapp.main
  (:require [crapp.template :as template]
            [crapp.utils :as utils]
            [crapp.config :as conf]))

(defn -main [& args]
  ;;TODO: make a CLI that allows the user to choose:
  ; - project name
  ; - template
  ; - one component from each component group for the chosen template
  )

